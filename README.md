
  # Email Response Generator UI

  This is a code bundle for Email Response Generator UI. The original project is available at https://www.figma.com/design/pNgpkNPcdpwwsHQZiUgeS9/Email-Response-Generator-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  